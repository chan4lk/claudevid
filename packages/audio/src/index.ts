// Public exports for @claudevid/audio (design.md's API Changes section).
//
// This is a stub: only the shared shapes exist so far (T2). `synthesize`, `computeAudioDurations`,
// `resolveCacheRoot`, and `installModels` are added by later tasks as tts.ts, durations.ts,
// cache-root.ts, and models.ts land.

export type { SynthesisRequest, CachedSynthesis, AudioDurationsOptions } from "./types.js";
