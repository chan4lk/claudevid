{"tier": "thin",
 "domains": [],
 "rationale": "One subsystem (text renderer) changes with localized additive logic for placement anchoring, no open question alters the file list, and undoing it is a clean revert with no backward-compatibility risk since the default align: \"left\" is unchanged.",
 "signals": {"blast_radius": "Text placement logic in packages/renderer-canvas/src/index.ts (both static draw and motion-transform branches) and test updates; no schema changes or edits to resolveAxis or paintTextLayer", "unresolved": "Whether motion-transform pivots should also shift to the new anchor or remain box-relative — a design choice needed during implementation but does not change the file list", "irreversibility": "Pure git revert with no migration; the default align: \"left\" behavior is explicitly preserved, so existing specs remain unaffected"}}
